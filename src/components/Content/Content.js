import { Fragment } from 'react';
import SocialLinks from './SocialLinks/SocialLinks';
const Content = (props) => {
  return (
    <Fragment>
      <SocialLinks />
    </Fragment>
  );
};

export default Content;
